import { TostringpersonPipe } from './tostringperson.pipe';

describe('TostringpersonPipe', () => {
  it('create an instance', () => {
    const pipe = new TostringpersonPipe();
    expect(pipe).toBeTruthy();
  });
});
