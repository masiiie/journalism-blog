import { SlicecontentPipe } from './slicecontent.pipe';

describe('SlicecontentPipe', () => {
  it('create an instance', () => {
    const pipe = new SlicecontentPipe();
    expect(pipe).toBeTruthy();
  });
});
