import { TonormaldatePipe } from './tonormaldate.pipe';

describe('TonormaldatePipe', () => {
  it('create an instance', () => {
    const pipe = new TonormaldatePipe();
    expect(pipe).toBeTruthy();
  });
});
